import * as T from '../../types';
import { TokenTracker } from '../utils';
export declare function parseCondition(tt: TokenTracker, parseExpr: (prevExpr: T.Expr, meta?: any) => T.Expr, prevExpr: T.ConditionalExpr): T.ConditionalExpr;
