import * as T from '../../types';
import { TokenTracker, Scope } from '../utils';
export declare function parseLibDirectMethodMapping(tt: TokenTracker, parseExpr: T.ExpressionParsingFunction, scope: Scope): Scope;
