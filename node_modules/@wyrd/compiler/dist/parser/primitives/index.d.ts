import * as T from '../../types';
import { TokenTracker, Scope } from '../utils';
export declare function parsePrimitive(tt: TokenTracker, parseExpr: T.ExpressionParsingFunction, scope: Scope, prevExpr?: T.Expr): T.Expr;
