import DataType from './DataType';
export default class Parameter {
    list: DataType[];
    static of(...types: DataType[]): Parameter;
    static from(types: DataType[]): Parameter;
    static Void(): Parameter;
    constructor(...types: DataType[]);
    get length(): number;
    toString(): string;
    matches(p: Parameter, receiver?: DataType): boolean;
}
