export default class GenericType {
    name: string;
    readonly typeParameters: Array<TypeParameter>;
    private typeParameterNames;
    constructor(name: string);
    declareTypeParameter(name: string): void;
}
declare class TypeParameter {
    name: string;
    order: number;
    constructor(name: string, order: number);
    toString(): string;
}
export {};
