import MethodObject from './Scope.MethodObject';
export default class MethodType {
    receiver: string;
    methods: Map<string, MethodObject>;
    constructor(receiver: string);
    createMethod(name: string): MethodObject;
}
