import * as T from '../../types';
import { TokenTracker, Scope } from '../utils';
/**
 *  Invoke method via type, for instance: Num.toStr(123)
 *  is equivalent to: 123.toStr()
 */
export declare function parseReceiverAsType(tt: TokenTracker, parseExpr: (orevExpr?: T.Expr, meta?: any) => T.Expr, scope: Scope, prevExpr: T.TypeLiteral): T.MethodInvokeExpr;
