import * as T from '../../types';
import { TokenTracker, Scope } from '../utils';
/**
 *  Basic case of invoking method from values, for instance: 123.toStr()
 */
export declare function parseReceiverAsValue(tt: TokenTracker, parseExpr: (orevExpr?: T.Expr, meta?: any) => T.Expr, scope: Scope, prevExpr: T.Expr): T.MethodInvokeExpr;
