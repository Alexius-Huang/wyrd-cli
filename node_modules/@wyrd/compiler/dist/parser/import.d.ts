import * as T from '../types';
import { TokenTracker, Scope } from './utils';
export declare function parseImportExpr(tt: TokenTracker, parse: (tokens: Array<T.Token>, options: {
    rootDir: string;
    defaultScope?: Scope;
    defaultAST?: T.AST;
    mainFileOnly?: boolean;
    isLib?: boolean;
}) => {
    ast: T.AST;
    scope: Scope;
}, scope: Scope, rootDir: string): {
    scope: Scope;
    ast: T.AST;
};
