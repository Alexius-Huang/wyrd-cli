import * as T from '../../types';
import { TokenTracker, Scope } from '../utils';
export declare function parseParameters(tt: TokenTracker, parseExpr: T.ExpressionParsingFunction, scope: Scope): Array<T.Expr>;
