import * as T from '../types';
import { Scope } from './utils';
export declare function parse(tokens: Array<T.Token>, options: {
    rootDir: string;
    defaultScope?: Scope;
    defaultAST?: T.AST;
    mainFileOnly?: boolean;
    isLib?: boolean;
}): {
    ast: T.AST;
    scope: Scope;
};
