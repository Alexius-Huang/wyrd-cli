"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var fs = __importStar(require("fs"));
var path = __importStar(require("path"));
var lexer_1 = require("./lexer");
var parser_1 = require("./parser");
function includeLibrary(name, scope) {
    var libFile = path.join(__dirname, 'lib', name + ".wyrd");
    var content = fs.readFileSync(libFile, 'utf-8');
    var tokens = lexer_1.lex(content);
    return parser_1.parse(tokens, {
        rootDir: path.dirname(libFile),
        defaultScope: scope,
        isLib: true,
    });
}
exports.includeLibrary = includeLibrary;
